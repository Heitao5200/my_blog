# 第七章代码：DeepSeek 真实运行验证

验证时间：2026-09-18（北京时间）。本次由助手实际运行全部 15 个编号脚本，不代表学习者已经完成课程作业。

所有模型入口统一读取本目录 `.env`，使用硅基流动的 `deepseek-ai/DeepSeek-V4-Flash`，API 地址为 `https://api.siliconflow.cn/v1`。未使用魔搭，未启动本地模型。12 使用已配置的 Tavily，未使用 SerpAPI。

| 脚本 | 结果 | 实际观察 |
| --- | --- | --- |
| 00_check_environment.py | 通过 | 依赖、配置可读取 |
| 01_quick_start.py | 通过 | DeepSeek 返回中文回答，历史 2 条 |
| 02_llm_and_stream.py | 通过 | 普通与流式响应均返回文本 |
| 03_modelscope_provider.py | 通过 | 旧文件名保留，实际调用配置的 DeepSeek |
| 04_local_model.py | 通过 | 旧文件名保留，实际调用云端 DeepSeek |
| 05_message_config_agent.py | 通过 | 消息、配置、历史增删断言通过 |
| 06_simple_agent.py | 通过 | 记住小明；实际调用计算器得到 152；流式和工具增删完成 |
| 07_react_agent.py | 通过 | Action 调用计算器，Observation 为 112，然后 Finish |
| 08_reflection_agent.py | 通过 | 通用任务与代码任务的初稿、审查流程完成 |
| 09_plan_and_solve.py | 通过 | 计划三步，逐步得到 30、25、70 |
| 10_function_calling.py | 通过 | 原生工具调用实际执行，结果 10.0，模型给出最终回答 |
| 11_calculator_and_registry.py | 通过 | 函数和对象两种注册均运行 |
| 12_advanced_search.py | 通过 | Tavily 返回真实网页摘要和来源链接 |
| 13_tool_chain.py | 通过 | 两步串联计算得 120 |
| 14_async_tools.py | 通过 | 并发计算得到 4、4.0、112 |

上述入口退出码均为 0。修改后的 13 项离线回归测试也全部通过。

本机完整输出位于 `logs/live-20260918-001056/`，日志目录已被 Git 忽略。03、04 不再要求 MODELSCOPE 或 LOCAL 配置；仅保留原来的脚本文件名，避免之前的命令失效。

## 结果边界

- 这是本次配置和请求下的真实成功记录，外部服务之后仍可能超时或限流。
- 09 的模型输出中出现了 `</think>` 残留标签，程序正常完成并算出 70；日志保留原始输出，未把它伪造成干净文本。
- 08 生成的代码仅展示、未执行，其自我审查结论不等于正确性证明。示例输出对整数类型的理解仍需学习者核对。
- 04 已改为云端接口练习，因此不能用本记录证明本地部署成功。

---

# 首次整理时的离线验证（历史记录）

本记录描述整理代码时执行的检查，不是学习者的作业、截图或真实模型运行记录。

## 环境

- macOS，Python 3.14.3。
- 本目录独立 `.venv`，hello-agents 0.1.1；完整版本见 `requirements.lock.txt`。
- `python -m pip check`：No broken requirements found。

## 实际执行

| 检查 | 结果 |
| --- | --- |
| 全部 27 个 Python 文件语法编译 | 通过 |
| 15 个编号入口导入，无导入即执行 | 通过 |
| 00 环境检查 | 通过，未填模型或搜索配置 |
| 05 Message / Config / Agent | 通过，历史增加和清空断言通过 |
| 11 计算器与注册表 | 通过，函数与对象两条路径均执行 |
| 13 顺序工具链 | 通过，真实计算得到 120 |
| 14 并发工具 | 通过，真实计算得到 4、4.0、112 |
| verify_offline.py | 13 项测试全部通过 |
| 未配置模型时运行 01 | 明确报缺少配置，退出码 1，符合预期 |
| 从 `/tmp` 用绝对路径启动以上脚本 | 通过，不依赖当前工作目录 |
| .env / .venv 的 Git 忽略规则 | 通过 |

离线测试覆盖计算器非法输入、SimpleAgent 两种工具注册、流式历史、调用上限、ReAct 格式恢复与观察回填、Reflection 迭代和停止、Plan-and-Solve 两种计划格式及非法计划、原生函数调用的 tool_call_id 回传、搜索回退、工具链错误。

另有一项测试启动临时本地 HTTP 服务，使用真实安装的 OpenAI SDK 和 MyLLM 发送请求，验证普通与 SSE 流式响应解析。测试端口随机分配，测试后关闭。

日志保存在本机 `logs/`，该目录被 Git 忽略。

## 尚未验证

- 真实云模型响应、账号权限、模型名称、额度以及工具选择效果。
- ModelScope 真实服务、本地 Ollama / vLLM 服务。
- Tavily / SerpAPI 的真实搜索返回。

原因：当前整理目录的 `.env` 尚未填写这些服务的配置。测试替身只用于 `verify_offline.py`，日常编号脚本不会自动使用模拟响应。
